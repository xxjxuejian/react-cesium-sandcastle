---
alwaysApply: true
---

# React + Cesium 项目规范

- 描述项目

## 技术栈要求

- 框架: React 18 (Hooks), Vite, TypeScript.
- GIS: 使用 Cesium
- UI: Ant Design 5.x (使用 ConfigProvider 自定义主题).
- 样式: Tailwind CSS 3.4+ (用于布局和自定义间距).
- 状态管理: Zustand (轻量级状态).
- 路由: React Router 7.x (负责页面跳转)
- 国际化: react-i18next.

## 依赖管理

- 包管理器:pnpm
- 不要使用 npm 或 yarn

## 文件组织

### 目录结构

src/
├── api/ # 接口请求（如果需要从后端拉取案例列表）
├── assets/ # 静态资源（SCSS, 字体, 图片）
├── components/ # 公共业务组件
├── config/ # 全局配置
├── examples/ # 案例库：所有的 Cesium 具体实现代码
├── hooks/ # 自定义 Hook (如 useWindowSize, useCesiumControl)
├── i18n/ # 国际化配置
├── layouts/ # 页面布局
├── pages/ # 页面级组件
├── store/ # 状态管理 (Zustand)
├── styles/ # 全局样式
├── types/ # 全局 TypeScript 定义
├── utils/ # 工具函数 (坐标转换、格式化)
├── App.tsx # 根组件 (路由配置)
└── main.tsx # 入口文件
├── .env.development # 开发环境环境变量
├── .env.production # 生产环境环境变量
├── index.html
├── package.json
├── tsconfig.json
├── tailwind.config.js
└── vite.config.ts # Vite 配置

### 导入顺序

1. React 相关
2. 第三方库
3. 项目内部组件
4. 工具函数
5. 类型定义
6. 样式

## 编码规则

- **性能优化**: 避免在 Cesium 渲染循环中触发大量的 React 重新渲染。复杂的 Cesium 对象使用 useMemo 或 useRef 保持。
- **响应式**: 侧边栏菜单必须适配移动端（手机端隐藏为 Drawer 或折叠）。
- **i18n**: 所有 UI 文本（按钮、菜单名）必须使用 `t('key')`，翻译文件放在 `src/i18n/locales/`。
- **路径别名**: 使用 `@/` 指向 `src/`。

## Cesium 特点规则

- 初始化 Viewer 时，务必检查是否配置了 Cesium Ion Token。
- 所有的 Cesium 实例方法调用应在 `useEffect` 或 `resium` 的事件回调中进行。
- 处理热更新 (HMR) 导致的 Cesium 画布重复加载问题。

## 代码规范

### 组件规范

- 只使用函数组件,禁止类组件
- 组件名使用 PascalCase
- 文件名使用 kebab-case
- 使用命名导出,不用默认导出

示例:

```ts
// ❌ 错误
export default function userProfile() {}

// ✅ 正确
export const UserProfile = () => {};
```

### TypeScript 规范

- 所有组件必须有类型定义
- Props 使用 interface,不用 type
- 禁止使用 any,使用 unknown 或具体类型
- 函数返回值必须显式声明类型

示例:

```jsx
// ✅ 正确的组件定义
interface UserCardProps {
name: string;
email: string;
age?: number;
}

export const UserCard = ({ name, email, age }: UserCardProps): JSX.Element => {
return (
<div className="p-4 border rounded">
<h3 className="text-lg font-bold">{name}</h3>
<p className="text-gray-600">{email}</p>
{age && <p>Age: {age}</p>}
</div>
);
};
```

### 命名规范

- 变量和函数:camelCase
- 组件:PascalCase
- 常量:UPPER_SNAKE_CASE
- 文件名:kebab-case
- CSS 类名:Tailwind 原子类,不写自定义 CSS

### 异步处理

- 所有异步操作使用 async/await
- 禁止使用 .then() 链式调用
- 必须有 try-catch 错误处理

示例:

```ts
// ✅ 正确
const fetchUserData = async (userId: string): Promise<User> => {
  try {
    const response = await fetch(`/api/users/${userId}`);
    if (!response.ok) throw new Error("Failed to fetch user");
    return await response.json();
  } catch (error) {
    console.error("Error fetching user:", error);
    throw error;
  }
};
```

## 质量要求

### 错误处理

- API 调用必须有 try-catch
- 错误信息要对用户友好
- 记录错误日志

### 性能优化

- 列表渲染必须有 key 属性
- 避免在渲染函数中创建新对象或函数
- 使用 React.memo 优化不必要的重渲染
- 图片必须指定 width 和 height

### 代码质量

- 单个文件不超过 300 行
- 单个函数不超过 50 行
- 复杂逻辑必须有注释
- 关键函数必须有 JSDoc 注释
