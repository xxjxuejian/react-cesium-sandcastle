---
alwaysApply: true
---

# 文件组织

## 目录结构

src/
├── api/ # 接口请求（如果需要从后端拉取案例列表）
├── assets/ # 静态资源（SCSS, 字体, 图片）
├── components/ # 公共业务组件
├── config/ # 全局配置
├── examples/ # 案例库：所有的 Cesium 具体实现代码
├── hooks/ # 自定义 Hook (如 useWindowSize, useCesiumControl)
├── i18n/ # 国际化配置
├── layouts/ # 页面布局
├── pages/ # 页面级组件
├── store/ # 状态管理 (Zustand)
├── styles/ # 全局样式
├── types/ # 全局 TypeScript 定义
├── utils/ # 工具函数 (坐标转换、格式化)
├── App.tsx # 根组件 (路由配置)
└── main.tsx # 入口文件
├── .env.development # 开发环境环境变量
├── .env.production # 生产环境环境变量
├── index.html
├── package.json
├── tsconfig.json
├── tailwind.config.js
└── vite.config.ts # Vite 配置

## 导入顺序

1. React 相关
2. 第三方库
3. 项目内部组件
4. 工具函数
5. 类型定义
6. 样式
